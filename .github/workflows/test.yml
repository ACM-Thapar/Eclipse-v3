name: test

on:
  pull_request:
    branches:
      - main
      - master
      - v*

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - run: cd server && npm i && npm run test:ci
      
